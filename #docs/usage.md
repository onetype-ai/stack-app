# usage

## What this is

An empty application on `@onetype/stack-app-kit`. Rewrite this section first:
what the product is, who uses it, and which repository serves its `/api`.

## Today

No plugin ships. List each capability here as its plugin lands.
