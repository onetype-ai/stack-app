# architecture

## Built today

A kernel that starts whatever `src/plugins/` holds, and nothing in it yet.

## Decided, not built yet

One line a decision that shapes more than one plugin: tenancy, identity,
realtime, what the server owns. Delete a line when its decision is built.
