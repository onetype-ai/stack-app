# utils/

`Name.ts`, one a file, pure: no `ctx`, no React, no request. This one is
reachable by this plugin alone. Code shared by plugins goes in `src/utils/`.

```ts
export const ItemsKeys = {
    list: (): readonly unknown[] => ["items", "list"],
};
```

Every plugin keeps its cache keys this way.
